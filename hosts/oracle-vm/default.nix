# Host-specific configuration for Oracle Cloud VM
# This module can contain host-specific overrides
{ config, pkgs, lib, ... }:

{
  # Host-specific packages (if any)
  # home.packages = with pkgs; [ ];

  # Host-specific environment variables (if any)
  # home.sessionVariables = { };
}
